# NotionNext 博客部署指南

## 1. 准备工作

在开始部署前，请确保您已完成以下准备工作：

1. 已注册 [GitHub](https://github.com) 账号
2. 已注册 [Vercel](https://vercel.com) 账号
3. 已完成 Notion 数据库设置（参考 `Notion配置指南.md`）
4. 已获取 Notion API Token 和数据库 ID

## 2. Fork NotionNext 项目

1. 访问 [NotionNext GitHub 仓库](https://github.com/tangly1024/NotionNext)
2. 点击右上角的 "Fork" 按钮，将仓库复制到您的 GitHub 账号下
3. 等待 Fork 完成

## 3. 修改配置文件

有两种方式可以修改配置：

### 方式一：本地修改后提交

1. 将 Fork 后的仓库克隆到本地：
   ```bash
   git clone https://github.com/您的用户名/NotionNext.git
   cd NotionNext
   ```

2. 复制 `.env.example` 为 `.env.local`：
   ```bash
   cp .env.example .env.local
   ```

3. 编辑 `.env.local` 文件，填入 Notion 配置：
   ```
   NOTION_PAGE_ID=您的Notion数据库ID
   NOTION_ACCESS_TOKEN=您的Notion集成API密钥
   NEXT_PUBLIC_THEME=heo
   ```

4. 按需修改 `blog.config.js` 文件（参考 `NotionNext配置指南.md`）

5. 提交更改并推送到 GitHub：
   ```bash
   git add .
   git commit -m "Update configuration"
   git push
   ```

### 方式二：在 Vercel 部署时设置环境变量

直接在 Vercel 部署时设置环境变量，无需修改代码。

## 4. 在 Vercel 上部署

1. 登录 [Vercel](https://vercel.com)
2. 点击 "New Project" 创建新项目
3. 选择 "Import Git Repository" 并选择您 Fork 的 NotionNext 仓库
4. 在配置页面进行如下设置：
   - **Project Name**: 自定义项目名称
   - **Framework Preset**: Next.js
   - **Root Directory**: `/` (默认)
   - **Build Command**: 保持默认
   - **Output Directory**: 保持默认
   - **Environment Variables**: 添加以下环境变量
     ```
     NOTION_PAGE_ID=您的Notion数据库ID
     NOTION_ACCESS_TOKEN=您的Notion集成API密钥
     NEXT_PUBLIC_THEME=heo
     ```

5. 点击 "Deploy" 按钮开始部署
6. 等待部署完成（约 2-5 分钟）

## 5. 绑定自定义域名（可选）

如果您希望使用自己的域名，而不是 Vercel 提供的默认域名，可以进行以下操作：

1. 在 Vercel 项目页面，点击 "Settings" -> "Domains"
2. 输入您想要绑定的域名，点击 "Add"
3. 按照 Vercel 的指引，在您的域名服务商处添加相应的 DNS 记录
4. 等待 DNS 生效（最长可能需要 48 小时）

## 6. 验证部署

1. 访问部署后的网站（Vercel 提供的网址或您绑定的自定义域名）
2. 检查以下内容是否正常：
   - 网站能否正常加载
   - 文章是否显示
   - 主题是否为 Heo 主题
   - 图片是否正常显示
   - 各页面跳转是否正常

## 7. 博客更新与维护

### 内容更新
- 直接在 Notion 中编辑文章，网站会自动更新（取决于缓存设置，可能有几分钟延迟）

### 代码更新
如需更新 NotionNext 至最新版本：

1. 添加上游仓库：
   ```bash
   git remote add upstream https://github.com/tangly1024/NotionNext.git
   ```

2. 获取上游更新：
   ```bash
   git fetch upstream
   ```

3. 合并更新到您的主分支：
   ```bash
   git merge upstream/main
   ```

4. 解决可能的冲突，然后推送更新：
   ```bash
   git push
   ```

## 8. 常见问题排查

### 部署失败

- 检查环境变量是否正确设置
- 查看 Vercel 构建日志，找出具体错误
- 确保 Notion 数据库权限已正确设置

### 网站加载但没有内容

- 确认 Notion API 令牌是否有效
- 检查数据库 ID 是否正确
- 确保数据库中有已发布的文章

### 图片无法显示

- 检查 Notion 中的图片是否可公开访问
- 考虑使用图床服务存储图片

### 更新内容后网站不刷新

- 检查缓存设置（在 `blog.config.js` 中的 `NEXT_REVALIDATE_SECOND`）
- 手动触发 Vercel 重新部署 