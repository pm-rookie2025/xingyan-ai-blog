{
  "buildCommand": "npm install && cd NotionNext && npm run build",
  "outputDirectory": "NotionNext/.next",
  "framework": "nextjs",
  "routes": [
    { "src": "/(.*)", "dest": "/NotionNext/$1" }
  ]
} 