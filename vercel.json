{
  "buildCommand": "cd NotionNext && npm install && npm run build",
  "outputDirectory": "NotionNext/.next",
  "installCommand": "cd NotionNext && npm install",
  "framework": "nextjs",
  "routes": [
    { "src": "/(.*)", "dest": "/NotionNext/$1" }
  ]
} 